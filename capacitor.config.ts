import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.github.thisgavagai.tablemate',
  appName: 'Tabula Mensa',
  webDir: 'dist'
}

export default config
