/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/vue" />

// Injected by Vite's `define` at build time (see vite.config.mts). Holds the
// release tag in CI builds, the package.json version locally.
declare const __APP_VERSION__: string
